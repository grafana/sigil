1. Pull origin main latest and merge into the current branch
2. Resolve any conflicts automatically - only ask for more context from the user if you're really not sure how to resolve it
3. Format the code with: mise run format
4. Run lints and typechecks
5. Push the branch
